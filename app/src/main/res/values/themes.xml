<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Tfgdam" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Fuente personalizada -->
        <item name="android:fontFamily">@font/archivofont</item>
        <item name="android:statusBarColor">@color/red</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="colorOnPrimary">@android:color/white</item> <!-- Material3 usa esto para texto sobre primary -->
    </style>

    <style name="Theme.Tfgdam" parent="Base.Theme.Tfgdam" />
</resources>
